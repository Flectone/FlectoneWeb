---
title: LiteBans
description: Интеграция с плагином LiteBans проверяет мут игрока и не позволяет ему писать сообщение, а также отключает команды модерации FlectonePulse
authors:
  - TheFaser
---
<Callout type="warn" title="Предупреждение">
Лучше выключить команды самому в `command.yml`, потому что не всегда команды выключаются сами
</Callout>


# <include>../../../parts/words.mdx#setting</include>
<span><include>../../../parts/words.mdx#path</include> `integration.yml → litebans`</span>


<include>../../../files/integration.yml#litebans</include>

<include>../../../parts/enable.mdx</include>

### `disable_flectonepulse_ban`

Выключает команды `/ban`, `/banlist` и `/unban` в `FlectonePulse`

### `disable_flectonepulse_mute`

Выключает команды `/mute`, `/mutelist` и `/unmute` в `FlectonePulse`

### `disable_flectonepulse_warn`

Выключает команды `/warn`, `/warnlist` и `/unwarn` в `FlectonePulse`

### `disable_flectonepulse_kick`

Выключает команды `/kick` в `FlectonePulse`


# <include>../../../parts/words.mdx#permission</include>
<span><include>../../../parts/words.mdx#path</include> `permission.yml → integration.litebans`</span>


<include>../../../files/permission.yml#litebans</include>

<include>../../../parts/permission/permissionTier3.mdx</include>