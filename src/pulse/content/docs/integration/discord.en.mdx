---
title: Discord
description: Integration with Discord allows sending messages (Discord ↔ Minecraft) between the Minecraft server and a Discord server.
authors:
  - TheFaser
---
![discord message](/pulse/discordmessage.png)
![minecraft message](/pulse/discordminecraftmessage.png)

<Callout type="info" title="Information">

For the bot to work correctly, enable three settings for your bot in the [Discord Developer Portal](https://discord.com/developers/applications/)

![develop](/pulse/discorddevelopers.png)

Also grant the bot permissions for `Read Messages`, `Send Messages`, and `Use Webhooks`

</Callout>

<include>../../../parts/messageTag.en.mdx</include>

<br></br>

# <include>../../../parts/words.en.mdx#localization</include>
<span><include>../../../parts/words.en.mdx#path</include> `localizations → language.yml → integration.discord`</span>

<Tabs items={['en_us', 'ru_ru']}>
  <Tab value="en_us"><include>../../../files/localizations/en_us.yml#discord</include></Tab>
  <Tab value="ru_ru"><include>../../../files/localizations/ru_ru.yml#discord</include></Tab>
</Tabs>

### `null_player`

Message if the player entered via `custom_command` is not found

### `format_reply`

Message format for the `<reply>` tag when the message is a reply to another one

### `custom_command`

List of custom integration commands, where the key is the command name and the value is its message format

<Callout type="info" title="Example">

```yaml
custom_command:
  tps:
    content: "<tps>"
```

Command to get the `tps` value on the server. **Don't forget to create it in integration.yml**

</Callout>

#### `Placeholders`

You can use all placeholders that are used in the initial message for Minecraft
<Callout type="info" title="EXAMPLE FOR BAN MESSAGE">
There is a `<reason>` placeholder there, so I can use `<reason>` inside Discord messages
</Callout>

There are also placeholders that will DEFINITELY be replaced in any message
- `<final_message>` message sent to Minecraft
- `<final_clear_message>` message sent to Minecraft without unicode emojis
- `<player>` nickname of the player who sent the message
- `<message>` raw message written by the player
- `<plain_message>` formatted message written by the player
- `<reply>` formatted reply message, it will be empty if the main message is not a reply
- Obviously, all placeholders from `PlaceholderAPI` and `FlectonePulse` will also work

The message that will be sent from Discord to Minecraft has its own placeholders:

| Placeholder      | What it returns                       |
|------------------|---------------------------------------|
| `<name>`         | Global name of the Discord member     |
| `<global_name>`  | Global name of the Discord member     |
| `<nickname>`     | Nickname of the Discord member        |
| `<display_name>` | Display name of the Discord member    |
| `<user_name>`    | Discord member's tag                  |

### `info_channel`

List of channel IDs and their names for displaying some information, such as `TPS`

### `message_channel`

List of messages with their settings

```yaml
message_name:
  content: ""
  webhook_avatar: "https://mc-heads.net/avatar/<skin>/32.png"
    embed:
    color: ""
    title: ""
    url: ""
    author:
      name: ""
      url: ""
      icon_url: "https://mc-heads.net/avatar/<skin>/16.png"
    description: ""
    thumbnail: ""
    fields:
      - name: ""
        value: ""
        inline: false
    image: ""
    timestamp: true
    footer:
      text: ""
      icon_url: "https://mc-heads.net/avatar/<skin>/16.png"
```

<Callout type="info" title="Information">

    Message with all parameters
    ![discord](/pulse/discordembedpreview.png)

    If a parameter is empty or not written, it will not be used in the final message

</Callout>

<Accordions>
    <Accordion title="Message settings">
        #### `content`

        Message content
        ![discord content](/pulse/discordcontent.png)

        #### `webhook_avatar`

        Enable Discord webhook with the player's avatar. It is best to use `https://mc-heads.net/avatar/<skin>/32.png`
        ![discord webhook](/pulse/discordwebhook.png)

        #### `embed`

        Discord embed message
        ![discord embed](/pulse/discordembed.png)

        ##### `color`

        Color of the `embed` message

        ##### `title`

        Title of the `embed` message

        ##### `url`

        Link for the `embed` message

        ##### `author`

        <Accordions>
            <Accordion title="Author of the `embed` message">
                ###### `name`

                Author's name of the `embed` message

                ###### `url`

                Link to the author of the `embed` message

                ###### `icon_url`

                Link to the author's avatar of the `embed` message. If you change it to your own, the link must contain `<skin>`
            </Accordion>
        </Accordions>
        <br/>
        ##### `description`

        Description of the `embed` message

        ##### `thumbnail`

        Small image inside the `embed` message

        ##### `fields`

        <Accordions>
            <Accordion title="Bottom fields inside the `embed` message">
                ###### `name`

                Field name inside the `embed` message

                ###### `value`

                Field value inside the `embed` message

                ###### `inline`

                If enabled, the field will be placed in a column with other fields
            </Accordion>
        </Accordions>
        <br/>

        ##### `image`

        Main image of the `embed` message

        ##### `timestamp`

        If enabled, the `embed` message will include the creation time

        ##### `footer`

        <Accordions>
            <Accordion title="Bottom part of the `embed` message">
                ###### `text`

                Text in the bottom part of the `embed` message

                ###### `icon_url`

                Image in the bottom part of the `embed` message
            </Accordion>
        </Accordions>
    </Accordion>
</Accordions>

<br></br>

# <include>../../../parts/words.en.mdx#setting</include>
<span><include>../../../parts/words.en.mdx#path</include> `integration.yml → discord`</span>

<include>../../../files/integration.yml#discord</include>

<include>../../../parts/enable.en.mdx</include>

<Callout type="warn" title="Warning">
- Before enabling, insert the Discord bot **token**
- After enabling, it is **RECOMMENDED** to restart the server, otherwise the plugin may cause a freeze
</Callout>

### `ignore_all_bots`

If enabled, messages from any users who are bots will be ignored. Otherwise, only the current bot's own messages will be ignored

### `token`

[Token](https://discordgsm.com/guide/how-to-get-a-discord-bot-token) of the Discord bot for connection. You can use environment variables, for example `${VALUE}`

### `custom_command`

List of custom integration commands, the key is the command name and it can be anything. Commands can ONLY be informational; they do not and will not execute anything on the server

| Parameter     | Explanation                                                                    |
|---------------|--------------------------------------------------------------------------------|
| `need_player` | Whether to check the first command argument for a player name, e.g., `!ping TheFaser` |
| `aliases`     | List of aliases for using the command                                          |

<Callout type="info" title="Example">

```yaml
custom_command:
  tps:
    need_player: false
    aliases:
      - "!tps"
      - "!tickpersecond"
```

Command to get the `tps` value on the server using `!tps` or `!tickpersecond`. **Don't forget to create a message in localization**

</Callout>

### `presence`

![discord presence](/pulse/discordpresence.png)

<Accordions>
    <Accordion title="Bot status settings">
        #### `enable`

        Enables or disables custom bot status

        #### `status`

        | Mode             | Explanation                     |
        |------------------|---------------------------------|
        | `UNKNOWN`        | -                               |
        | `ONLINE`         | Online                          |
        | `DO_NOT_DISTURB` | Online, with do not disturb mode |
        | `IDLE`           | Online, but away                |
        | `INVISIBLE`      | Invisible                       |
        | `OFFLINE`        | Offline                         |

        #### `activity`

        Bot activity in Discord

        ##### `enable`

        Enables or disables activity

        ##### `type`

        | Type        | Explanation |
        |-------------|-------------|
        | `UNKNOWN`   | -           |
        | `STREAMING` | Streaming   |
        | `LISTENING` | Listening   |
        | `WATCHING`  | Watching    |
        | `CUSTOM`    | -           |
        | `COMPETING` | Competing   |

        ##### `name`

        Activity name

        ##### `url`

        Activity link
    </Accordion>
</Accordions>

### `channel_info`

![discord channel info](/pulse/discordchannelinfo.png)

<Accordions>
    <Accordion title="Info channels settings">
        #### `enable`

        Whether an info channel is needed

        <include>../../../parts/ticker.en.mdx</include>
    </Accordion>
</Accordions>

### `message_channel`

List of message types and [Channel IDs](https://support.discord.com/hc/en-us/articles/206346498-Where-can-I-find-my-User-Server-Message-ID-) in Discord

<Callout type="info" title="For example, I want a `/ban` command message to be sent from Minecraft to Discord">
<Steps>
    <Step>
        Copy the channel ID in Discord `1286666844358316083`
    </Step>
    <Step>
        Set `COMMAND_BAN: "1286666844358316083"`
    </Step>
</Steps>

```yaml
message_channel:
  COMMAND_BAN: 
    - "1286666844358316083"
```
</Callout>

Localization can be left unconfigured; by default, the message will be sent with the `<final_message>` format

<include>../../../parts/destination.en.mdx</include>

<include>../../../parts/sound.en.mdx</include>

<br></br>

# <include>../../../parts/words.en.mdx#permission</include>
<span><include>../../../parts/words.en.mdx#path</include> `permission.yml → integration.discord`</span>

<include>../../../files/permission.yml#discord</include>

<include>../../../parts/permission/permissionTier3.en.mdx</include>

<include>../../../parts/permission/sound.en.mdx</include>