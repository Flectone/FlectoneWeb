---
title: Условие
description: Тег condition используется для отображения текста по определённому условию
authors:
  - TheFaser
tag: "hytale"
---
Использование: `<condition:название>`

![condition](/pulse/hytale/message_format_condition.webp)

<Callout type="info" title="Информация">
    Как сделать своё условие? Например значок для тех, у кого есть право `server.donate`

    <Steps>
        <Step>
            В `message.yml` добавь условие в `format.condition.values`
            ```yaml
            values:
              is_donate:
                value: "server.donate"
                type: "PERMISSION"
            ```
        </Step>
        <Step>
            В `localizations → язык.yml` добавь значения в `message.format.condition.values`
            ```yaml
            values:
              is_donate:
                "true": "<color:#FFD700>★ "
                "false": ""
            ```
        </Step>
        <Step>
            Вставь `<condition:is_donate>` в любое сообщение, например в формат имени в `message.format.names.display`
            ```yaml
            display:
            - "<condition:is_donate><nickname>"
            ```
        </Step>
        <Step>
            Перезагрузи FlectonePulse с помощью `/flectonepulse reload`
        </Step>
    </Steps>

    Для `type: "NUMBER"` значения указываются промежутками (`0`, `50`, `100`), а для `type: "STRING"` конкретным текстом в нижнем регистре
</Callout>

<br></br>

# <include>../../../../../parts/words.mdx#localization</include>
<span><include>../../../../../parts/words.mdx#path</include> `localizations → язык.yml → message.format.condition`</span>

<Tabs items={['ru_ru', 'en_us']}>
    <Tab value="ru_ru"><include>../../../../../files/hytale/localizations/ru_ru.yml#condition</include></Tab>
    <Tab value="en_us"><include>../../../../../files/hytale/localizations/en_us.yml#condition</include></Tab>
</Tabs>

### `values`

Список всех условий, где ключом является название, а значением параметры с их локализацией

Значение каждого параметра можно писать одной строкой или списком строк

Все параметры должны указываться в нижнем регистре, например у `player_name` ник `TheFaser`, а указывать нужно `thefaser`

Во всех condition можно использовать поле `default`, которое будет использоваться, если другие значения не нашлись. Это можно воспринимать как `else` у `if` конструкции

Если condition является `NUMBER`, то значения указываются в промежутках. Например condition это `%player_ping%` и определены значения для `0`, `50`, `100`, а у игрока пинг = `69`, значит локализация будет использовано из `50`

<br></br>

# <include>../../../../../parts/words.mdx#setting</include>
<span><include>../../../../../parts/words.mdx#path</include> `message.yml → format.condition`</span>

<include>../../../../../files/hytale/message.yml#condition</include>

<include>../../../../../parts/enable.mdx</include>

### `values`

Список всех условий с настройкой, где ключом является название

#### `value`

Текст, который будет проверяться на значение. Это строка и она может быть любой `"%player_name% %player_world%"` или `"1234"`

#### `type`

Тип условия

| Тип                | Объяснение                                   |
|--------------------|----------------------------------------------|
| `STRING`           | Строка, которая будет форматировариться и значение которой будет проверяться         |
| `NUMBER`           | Число, которое будет форматироваться и парситься, значение которого проверяется в промежутке             |
| `PERMISSION`       | Право, которое проверяется на наличие                              |

<br></br>

# <include>../../../../../parts/words.mdx#permission</include>
<span><include>../../../../../parts/words.mdx#path</include> `permission.yml → message.format.condition`</span>

<include>../../../../../files/hytale/permission.yml#condition</include>

<include>../../../../../parts/permission/permissionTier3.mdx</include>

### `values`

Список всех условий с их отдельными правами на отображение