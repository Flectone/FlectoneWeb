---
title: Прокси
description: Используется для синхронизации нескольких серверов с помощью Redis
authors:
  - TheFaser
tag: "hytale"
---

<br></br>

# <include>../../../../parts/words.mdx#setting</include>
<span><include>../../../../parts/words.mdx#path</include> `config.yml → proxy`</span>

<include>../../../../files/hytale/config.yml#proxy</include>

### `clusters`

Список кластеров, к которым должен быть подключён сервер. Используется только тогда, когда включен режим прокси.
Если список пустой, то сервер получает все сообщения с других серверов

<Callout type="info" title="Как использовать?">
Нужно вписать название кластера на всех серверах, где сообщение будет получено и отправлено
```yaml
clusters: 
   - "test_cluster"
```

Так сервера связываются между собой и сообщения будут только между этими серверами
</Callout>

### `bungeecord`

<Callout type="error" title="">
    Не поддерживается в Hytale
</Callout>

### `velocity`

<Callout type="error" title="">
    Не поддерживается в Hytale
</Callout>

### `redis`

Используется в качестве коммуникаций между разными серверами. Для `user` и `password` можно использовать environment variables, например `${VALUE}`

| Поле       | Объяснение                                                            |
|------------|-----------------------------------------------------------------------|
| `enable`   | Включает работу `Redis`                                               |
| `host`     | Хост (айпи) сервера, на котором запущен `Redis`                       |
| `port`     | Порт сервера, на котором запущен `Redis`                              |
| `ssl`      | Включает использование `SSL`                                          |
| `user`     | Имя пользователя в `Redis`, если его нет, то можно оставить пустым    |
| `password` | Пароль пользователя в `Redis`, если его нет, то можно оставить пустым |
