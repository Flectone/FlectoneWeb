---
title: Colors
description: The fcolor tag is used for individual color settings for each player
authors:
  - TheFaser
---
Usage: `<fcolor:number>`

![color message](/pulse/colormessage.png)

<include>../../../../parts/fcolor.en.mdx</include>

<br></br>

# <include>../../../../parts/words.en.mdx#setting</include>
<span><include>../../../../parts/words.en.mdx#path</include> `message.yml → format.fcolor`</span>

<include>../../../../files/message.yml#fcolor</include>

<include>../../../../parts/enable.en.mdx</include>

### `default_colors`

List of all default colors used. Each digit represents a specific color. By default: `<fcolor:1>` is used for the primary color, `<fcolor:2>` is used for the primary color with highlighting (e.g., names), `<fcolor:3>` for local chat messages, and `<fcolor:4>` for global chat messages.

<Callout type="info" title="For example, if there is a color:">
    ```yaml
    1: "<gradient:#A6D8FF:#8CC8FF>"
    ```
    `1` is the number for the color `<gradient:#A6D8FF:#8CC8FF>` <br/> <br/>
    To use this tag, you need to write `<fcolor:1>`
</Callout>

<br></br>

# <include>../../../../parts/words.en.mdx#permission</include>
<span><include>../../../../parts/words.en.mdx#path</include> `permission.yml → message.format.fcolor`</span>

<include>../../../../files/permission.yml#fcolor</include>

<include>../../../../parts/permission/permissionTier3.en.mdx</include>

### `colors`

Available `<fcolor:...>` options and their corresponding permissions