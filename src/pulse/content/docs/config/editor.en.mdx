---
title: Web Editor
description: Web editing for FlectonePulse. The backend server runs where FlectonePulse is installed.
authors:
  - TheFaser
---
![editor](/pulse/flectonepulseeditor.gif)

<br></br>

# <include>../../../parts/words.en.mdx#setting</include>
<span><include>../../../parts/words.en.mdx#path</include> `config.yml → editor`</span>

<include>../../../files/config.yml#editor</include>

### `host`

The host (IP) of the current server; a domain name can also be specified.

### `https`

Enables `https` operation instead of `http`.

### `port`

An available port on the current server.