---
title: /flectonepulse
description: Main command of the FlectonePulse plugin
authors:
  - TheFaser
tag: "minecraft"
---
![command flectonepulse](/pulse/command_flectonepulse.webp)

<Callout type="warn" title="Warning">

    On older versions (1.9 and 1.10 specifically), it has been noticed that players might be kicked from the server during a reload, but this does not affect the plugin's operation.

</Callout>

The command is used as `/flectonepulse type [file]`, where type is one of the actions below

| Type         | Explanation                                                                                                                                    |
|--------------|------------------------------------------------------------------------------------------------------------------------------------------------|
| `reload`     | Reloads `FlectonePulse` together with all configs                                                                                              |
| `dump`       | Uploads information about the server and the plugin to `pastes.dev` and returns a link. It is convenient to send it to support if something does not work |
| `editor`     | Starts the web editor for configs and returns a link to it. `host` and `port` must be configured in `command.yml`                               |
| `export`     | Saves the `FlectonePulse` folder into a `.zip` archive next to the configs, without the `libraries`, `backups`, `minecraft` folders and `.db` database files |
| `export_all` | The same as `export`, but with all files                                                                                                        |
| `import`     | Unpacks a `.zip` archive back into the `FlectonePulse` folder and reloads the plugin. The archive must be in the `FlectonePulse` folder          |

<Callout type="info" title="Information">
    The second argument can be the file name for `export`, `export_all` and `import`, for example `/flectonepulse export myconfig`. If it is not written, the name `export_date_time` will be used
</Callout>

<br></br>

# <include>../../../../parts/words.en.mdx#localization</include>

<span><include>../../../../parts/words.en.mdx#path</include> `localizations → language.yml → command.flectonepulse`</span>

<Tabs items={['en_us', 'ru_ru']}>
    <Tab value="en_us"><include>../../../../files/minecraft/localizations/en_us.yml#flectonepulse</include></Tab>
    <Tab value="ru_ru"><include>../../../../files/minecraft/localizations/ru_ru.yml#flectonepulse</include></Tab>
</Tabs>

### `dump_error`

Message when a dump upload error occurs

### `null_host_editor`

Message when the `host` field in `command.yml` is empty

### `null_port_editor`

Message when the `port` specified in `command.yml` is occupied or unavailable

### `null_file`

Message when the file for `import` is not found in the `FlectonePulse` folder

### `file_exist`

Message when the file for `export` already exists in the `FlectonePulse` folder

### `format_false`

Message when an error occurs

### `format_true`

The format of the message that will be sent upon reloading the entire `FlectonePulse`

### `format_dump`

The format of the message that will be sent upon successful dump upload

### `format_starting`

The format of the message that will be sent when an action starts, where `<type>` is replaced with its name

### `format_editor`

The format of the message that will be sent upon a successful web server launch

### `format_export`

The format of the message that will be sent upon a successful file export

### `format_import`

The format of the message that will be sent upon a successful file import

<br></br>

# <include>../../../../parts/words.en.mdx#setting</include>
<span><include>../../../../parts/words.en.mdx#path</include> `command.yml → flectonepulse`</span>

<include>../../../../files/minecraft/command.yml#flectonepulse</include>

<include>../../../../parts/enable.en.mdx</include>

### `execute_in_main_thread`

Whether the entire reload will be executed in the server's main thread

<Callout type="info" title="Information">

    This may resolve reload issues on `Bukkit`, `Spigot`, `Mohist`, or other old/non-standard cores.

</Callout>

<include>../../../../parts/aliases.en.mdx</include>

### `editor`

Web editing for FlectonePulse. The backend server runs where FlectonePulse is installed

![editor](/pulse/command_flectonepulse_editor.webp)

<Accordions>
    <Accordion title="Editor settings">
        #### `host`

        The host (IP) of the current server; a domain name can also be specified.

        #### `https`

        Enables `https` operation instead of `http`.

        #### `port`

        An available port on the current server.
    </Accordion>
</Accordions>

<include>../../../../parts/destination.en.mdx</include>

<include>../../../../parts/cooldown.en.mdx</include>

<include>../../../../parts/sound.en.mdx</include>

<br></br>

# <include>../../../../parts/words.en.mdx#permission</include>
<span><include>../../../../parts/words.en.mdx#path</include> `permission.yml → command.flectonepulse`</span>

<include>../../../../files/minecraft/permission.yml#flectonepulse</include>

<include>../../../../parts/permission/permissionTier3.en.mdx</include>

<include>../../../../parts/permission/cooldown.en.mdx</include>

<include>../../../../parts/permission/sound.en.mdx</include>