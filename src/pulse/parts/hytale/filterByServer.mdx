### `filter_by_server`

Если включено, то эта модерация будет работать только на этом сервере. Все команды, которые связаны с этой модерацией, также будут фильтроваться относительно значения `server` в [config.yml](/pulse/docs/hytale/config#server)